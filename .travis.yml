language: cpp

dist: focal

before_install:
  - sudo apt-get update
  - sudo apt-get -y install libiniparser-dev

compiler:
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
